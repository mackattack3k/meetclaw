{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shared capability for the main and settings windows",
  "windows": ["main", "settings"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
